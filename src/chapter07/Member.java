package chapter07;

public class Member {
	//사용하는 법은 어렵지 않다.
	Member m = new Member(); // 자료 타입이 Member 인것.
	Member m2 = new Member(); 
	Systme.out.println(m.hashCode());
	Systme.out.println(m2.hashCode());

}
